# 实验设备管理平台 - 前端项目安装指南

## 依赖安装命令

在项目根目录下运行以下命令安装依赖：

```bash
npm install
```

## 运行项目

安装完依赖后，运行以下命令启动项目：

```bash
npm run serve
```

项目将在 http://localhost:8090 启动。

## 构建项目

运行以下命令构建生产版本：

```bash
npm run build
```

## 项目结构

```
lab-equipment-front/
├── public/
│   ├── favicon.svg          # 自定义图标
│   └── index.html           # HTML 模板
├── src/
│   ├── api/
│   │   └── request.js       # axios 配置
│   ├── assets/
│   │   └── logo.svg         # 自定义 logo
│   ├── components/          # 组件
│   ├── router/
│   │   └── index.js         # 路由配置
│   ├── views/               # 页面
│   │   └── Login.vue        # 登录页面
│   ├── App.vue              # 根组件
│   └── main.js              # 入口文件
├── package.json             # 依赖配置
└── vue.config.js            # Vue 配置
```

## 主要功能

- 登录页面
- 跨域配置
- Token 认证
- Element Plus UI 组件库

## 注意事项

1. 确保后端服务运行在 http://localhost:8100
2. 登录成功后会跳转到 /dashboard 页面
3. Token 会存储在 localStorage 中
4. PowerShell策略问题，使用：Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process 解决